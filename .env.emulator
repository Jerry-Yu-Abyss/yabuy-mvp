# 只給 `npm run dev:emu`（vite --mode emulator）使用。
#
# 用 Vite 的 mode 機制而不是 cross-env，是為了不新增依賴，
# 而且 Windows 的 npm script 不支援 POSIX 的 `VAR=1 cmd` 寫法。
#
# 這個檔案不含任何機密：它只是一個開關，告訴前端連本機 emulator。
# src/firebase.js 另外用 import.meta.env.DEV 當外層守衛，
# 所以正式建置(vite build)不可能讀到這裡、也不可能連上 emulator。
VITE_USE_EMULATOR=1
